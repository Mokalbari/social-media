/* Ce loading state va s'appliquer à toutes les routes enfant de /users
Si un UI différent doit s'appliquer, alors il nécessaire de créer un loading plus bas et nesté
*/

export default function Loading() {
  return <p>Loading user data...</p>
}
